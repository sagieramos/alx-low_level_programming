#include "main.h"
/**
 * _memset - fills memory with constant byte.
 * @s: array of char
 * @b: array of chat
 * @n: number of bytes
 * Return:(s)
 */
void *_memset(void *s, int c, size_t n)
{
char *p = s;

while (n > 0)
{
*p = b;
p++;
n--;
}
return (s);
}
