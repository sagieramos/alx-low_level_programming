#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{       char msgchar[] = "_putchar";
	int msg;
	for (msg = 0; msg <= 7; msg++)
	{
		_putchar(msgchar[msg]);
	}
	_putchar('\n');
	return (0);
}
